---
title: getting started
---

## how the vault works

markdown files in `content/vault/` are automatically rendered as pages. add new `.md` files and they'll show up in the sidebar.

### features

- **file tree sidebar** — browse all vault pages
- **google auth** — only @caretta.so emails can access
- **markdown rendering** — write naturally, it just works
